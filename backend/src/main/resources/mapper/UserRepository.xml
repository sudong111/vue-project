<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.repository.UserRepository">

    <!-- username으로 사용자를 찾는 쿼리 -->
    <select id="findByUsername" resultType="com.example.backend.model.User">
        SELECT id, username, password, authority, email, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 사용자 정보를 저장하는 쿼리 -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, authority, email)
        VALUES (#{username}, #{password}, #{authority}, #{email})
    </insert>

</mapper>
